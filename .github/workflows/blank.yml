name: CI
on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest-arm64
    steps:
      - uses: actions/checkout@v4
      - name: Run a multi-line script
        run: |
          mkfs.ext2
